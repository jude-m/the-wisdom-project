{
  "name": "wisdom-research-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "tsc",
    "start:node": "npm run build && node dist/src/node.js",
    "typecheck": "tsc --noEmit",
    "bench": "npm run build && node dist/bench/bench.js"
  },
  "dependencies": {
    "@hono/node-server": "^1.14.0",
    "hono": "^4.8.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0",
    "wrangler": "^4.0.0"
  }
}
