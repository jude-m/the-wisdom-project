/// Represents the display mode for the dual-pane reader
enum ColumnDisplayMode {
  /// Display only Pali content in a single column
  paliOnly,

  /// Display only Sinhala content in a single column
  sinhalaOnly,

  /// Display both Pali and Sinhala content side by side
  both,
}
