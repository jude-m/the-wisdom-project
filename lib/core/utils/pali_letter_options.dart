/// Moved to `package:wisdom_shared/src/text/pali_letter_options.dart`.
///
/// Travels with `pali_conjunct_transformer.dart` — the transformer takes this
/// object as its only argument, so the two cannot live on opposite sides of the
/// `wisdom_shared` boundary. See that file for why the move happened.
library;

export 'package:wisdom_shared/wisdom_shared.dart' show PaliLetterOptions;
