include: package:lints/recommended.yaml

linter:
  rules:
    # Matches the app's house style (CLAUDE.md: "Always use const").
    - prefer_const_constructors
    - prefer_const_declarations
    - prefer_const_literals_to_create_immutables
