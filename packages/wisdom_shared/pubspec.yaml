name: wisdom_shared
description: Shared pure-Dart logic for The Wisdom Project (client + server)
version: 0.1.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dev_dependencies:
  test: ^1.24.0
  lints: ^3.0.0
